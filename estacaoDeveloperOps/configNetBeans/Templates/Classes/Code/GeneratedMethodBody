<#--
A built-in Freemarker template (see http://freemarker.sourceforge.net) used for
filling the body of methods generated by the IDE. When editing the template,
the following predefined variables, that will be then expanded into
the corresponding values, could be used together with Java expressions and
comments:
${method_return_type}       retorna o tipo do metodo criado
${default_return_value}     valor retornado por padrão pelo metodo
${method_name}              nome do metodo criado
${class_name}               nome completo qualificado da Classe
${simple_class_name}        nome simples da Classe
-->
throw new java.lang.UnsupportedOperationException("O METODO AINDA NÃO FOI IMPLEMENTADO.");
